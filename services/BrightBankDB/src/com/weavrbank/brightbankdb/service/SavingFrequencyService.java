/*Copyright (c) 2015-2016 wavemaker-com All Rights Reserved.
 This software is the confidential and proprietary information of wavemaker-com You shall not disclose such Confidential Information and shall use it only in accordance
 with the terms of the source code license agreement you entered into with wavemaker-com*/
package com.weavrbank.brightbankdb.service;

/*This is a Studio Managed File. DO NOT EDIT THIS FILE. Your changes may be reverted by Studio.*/

import java.io.OutputStream;
import java.util.List;
import java.util.Map;

import javax.validation.Valid;

import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;

import com.wavemaker.runtime.commons.file.model.Downloadable;
import com.wavemaker.runtime.data.exception.EntityNotFoundException;
import com.wavemaker.runtime.data.export.DataExportOptions;
import com.wavemaker.runtime.data.export.ExportType;
import com.wavemaker.runtime.data.expression.QueryFilter;
import com.wavemaker.runtime.data.model.AggregationInfo;

import com.weavrbank.brightbankdb.SavingFrequency;
import com.weavrbank.brightbankdb.WealthGoals;

/**
 * Service object for domain model class {@link SavingFrequency}.
 */
public interface SavingFrequencyService {

    /**
     * Creates a new SavingFrequency. It does cascade insert for all the children in a single transaction.
     *
     * This method overrides the input field values using Server side or database managed properties defined on SavingFrequency if any.
     *
     * @param savingFrequency Details of the SavingFrequency to be created; value cannot be null.
     * @return The newly created SavingFrequency.
     */
    SavingFrequency create(@Valid SavingFrequency savingFrequency);


	/**
     * Returns SavingFrequency by given id if exists.
     *
     * @param savingfrequencyId The id of the SavingFrequency to get; value cannot be null.
     * @return SavingFrequency associated with the given savingfrequencyId.
	 * @throws EntityNotFoundException If no SavingFrequency is found.
     */
    SavingFrequency getById(Integer savingfrequencyId);

    /**
     * Find and return the SavingFrequency by given id if exists, returns null otherwise.
     *
     * @param savingfrequencyId The id of the SavingFrequency to get; value cannot be null.
     * @return SavingFrequency associated with the given savingfrequencyId.
     */
    SavingFrequency findById(Integer savingfrequencyId);

	/**
     * Find and return the list of SavingFrequencies by given id's.
     *
     * If orderedReturn true, the return List is ordered and positional relative to the incoming ids.
     *
     * In case of unknown entities:
     *
     * If enabled, A null is inserted into the List at the proper position(s).
     * If disabled, the nulls are not put into the return List.
     *
     * @param savingfrequencyIds The id's of the SavingFrequency to get; value cannot be null.
     * @param orderedReturn Should the return List be ordered and positional in relation to the incoming ids?
     * @return SavingFrequencies associated with the given savingfrequencyIds.
     */
    List<SavingFrequency> findByMultipleIds(List<Integer> savingfrequencyIds, boolean orderedReturn);

    /**
     * Find and return the SavingFrequency for given name  if exists.
     *
     * @param name value of name; value cannot be null.
     * @return SavingFrequency associated with the given inputs.
     * @throws EntityNotFoundException if no matching SavingFrequency found.
     */
    SavingFrequency getByName(String name);

    /**
     * Updates the details of an existing SavingFrequency. It replaces all fields of the existing SavingFrequency with the given savingFrequency.
     *
     * This method overrides the input field values using Server side or database managed properties defined on SavingFrequency if any.
     *
     * @param savingFrequency The details of the SavingFrequency to be updated; value cannot be null.
     * @return The updated SavingFrequency.
     * @throws EntityNotFoundException if no SavingFrequency is found with given input.
     */
    SavingFrequency update(@Valid SavingFrequency savingFrequency);


    /**
     * Partially updates the details of an existing SavingFrequency. It updates only the
     * fields of the existing SavingFrequency which are passed in the savingFrequencyPatch.
     *
     * This method overrides the input field values using Server side or database managed properties defined on SavingFrequency if any.
     *
     * @param savingfrequencyId The id of the SavingFrequency to be deleted; value cannot be null.
     * @param savingFrequencyPatch The partial data of SavingFrequency which is supposed to be updated; value cannot be null.
     * @return The updated SavingFrequency.
     * @throws EntityNotFoundException if no SavingFrequency is found with given input.
     */
    SavingFrequency partialUpdate(Integer savingfrequencyId, Map<String, Object> savingFrequencyPatch);

    /**
     * Deletes an existing SavingFrequency with the given id.
     *
     * @param savingfrequencyId The id of the SavingFrequency to be deleted; value cannot be null.
     * @return The deleted SavingFrequency.
     * @throws EntityNotFoundException if no SavingFrequency found with the given id.
     */
    SavingFrequency delete(Integer savingfrequencyId);

    /**
     * Deletes an existing SavingFrequency with the given object.
     *
     * @param savingFrequency The instance of the SavingFrequency to be deleted; value cannot be null.
     */
    void delete(SavingFrequency savingFrequency);

    /**
     * Find all SavingFrequencies matching the given QueryFilter(s).
     * All the QueryFilter(s) are ANDed to filter the results.
     * This method returns Paginated results.
     *
     * @deprecated Use {@link #findAll(String, Pageable)} instead.
     *
     * @param queryFilters Array of queryFilters to filter the results; No filters applied if the input is null/empty.
     * @param pageable Details of the pagination information along with the sorting options. If null returns all matching records.
     * @return Paginated list of matching SavingFrequencies.
     *
     * @see QueryFilter
     * @see Pageable
     * @see Page
     */
    @Deprecated
    Page<SavingFrequency> findAll(QueryFilter[] queryFilters, Pageable pageable);

    /**
     * Find all SavingFrequencies matching the given input query. This method returns Paginated results.
     * Note: Go through the documentation for <u>query</u> syntax.
     *
     * @param query The query to filter the results; No filters applied if the input is null/empty.
     * @param pageable Details of the pagination information along with the sorting options. If null returns all matching records.
     * @return Paginated list of matching SavingFrequencies.
     *
     * @see Pageable
     * @see Page
     */
    Page<SavingFrequency> findAll(String query, Pageable pageable);

    /**
     * Exports all SavingFrequencies matching the given input query to the given exportType format.
     * Note: Go through the documentation for <u>query</u> syntax.
     *
     * @param exportType The format in which to export the data; value cannot be null.
     * @param query The query to filter the results; No filters applied if the input is null/empty.
     * @param pageable Details of the pagination information along with the sorting options. If null exports all matching records.
     * @return The Downloadable file in given export type.
     *
     * @see Pageable
     * @see ExportType
     * @see Downloadable
     */
    Downloadable export(ExportType exportType, String query, Pageable pageable);

    /**
     * Exports all SavingFrequencies matching the given input query to the given exportType format.
     *
     * @param options The export options provided by the user; No filters applied if the input is null/empty.
     * @param pageable Details of the pagination information along with the sorting options. If null exports all matching records.
     * @param outputStream The output stream of the file for the exported data to be written to.
     *
     * @see DataExportOptions
     * @see Pageable
     * @see OutputStream
     */
    void export(DataExportOptions options, Pageable pageable, OutputStream outputStream);

    /**
     * Retrieve the count of the SavingFrequencies in the repository with matching query.
     * Note: Go through the documentation for <u>query</u> syntax.
     *
     * @param query query to filter results. No filters applied if the input is null/empty.
     * @return The count of the SavingFrequency.
     */
    long count(String query);

    /**
     * Retrieve aggregated values with matching aggregation info.
     *
     * @param aggregationInfo info related to aggregations.
     * @param pageable Details of the pagination information along with the sorting options. If null exports all matching records.
     * @return Paginated data with included fields.
     *
     * @see AggregationInfo
     * @see Pageable
     * @see Page
	 */
    Page<Map<String, Object>> getAggregatedValues(AggregationInfo aggregationInfo, Pageable pageable);

    /*
     * Returns the associated wealthGoalses for given SavingFrequency id.
     *
     * @param id value of id; value cannot be null
     * @param pageable Details of the pagination information along with the sorting options. If null returns all matching records.
     * @return Paginated list of associated WealthGoals instances.
     *
     * @see Pageable
     * @see Page
     */
    Page<WealthGoals> findAssociatedWealthGoalses(Integer id, Pageable pageable);

}
